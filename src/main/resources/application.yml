spring:
  datasource:
    url: jdbc:mysql://192.168.0.112:3307/clarionerp?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: burite
    password: burite123

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: false
    locations: classpath:db/migration

  server:
    port: 8081
